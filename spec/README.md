# Spec Directory

The canonical UKS Bento Box schema is maintained at:

    packages/core/schemas/uks.schema.json

The `schema_draft.json` in this directory is a working draft for future schema evolution.
